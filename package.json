{
  "name": "alpinejs-plugin-template",
  "version": "1.0.0",
  "description": "Template for building Alpine.js plugins.",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./cdn": "./dist/cdn.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rolldown -c",
    "watch": "rolldown -c --watch",
    "prepack": "npm run build",
    "release:patch": "npm version patch -m \"chore(release): %s\" && npm run release",
    "release:minor": "npm version minor -m \"chore(release): %s\" && npm run release",
    "release:major": "npm version major -m \"chore(release): %s\" && npm run release",
    "release:publish": "npm publish --access public",
    "release:push": "git push --follow-tags origin main",
    "release": "npm run build && npm run release:publish && npm run release:push"
  },
  "peerDependencies": {
    "alpinejs": "^3.0.0"
  },
  "devDependencies": {
    "rolldown": "^1.0.0-rc.1"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "license": "MIT"
}